# 🖥️ PySide6 UI Router

Простое desktop-приложение на **PySide6**, демонстрирующее систему управления интерфейсом через `QStackedLayout`.
Проект реализует переключение экранов (страниц) без пересоздания окна — аналог **Router / View Manager** в веб-приложениях.

---

## 📌 Возможности

* Переключение страниц (`Home`, `Settings`)
* Централизованное управление layout через `UIManager`
* Архитектура страниц (каждая страница — отдельный Layout)
* Использование `QMainWindow` + `CentralWidget`
* Готово к расширению (добавление новых экранов)
* Простая и понятная структура

---

## 🧱 Архитектура

Приложение использует:

* **QMainWindow** — базовое окно
* **QStackedLayout** — хранение и переключение страниц
* **UIManager** — менеджер layout (router)
* **Layouts** — отдельные экраны (`HomeLayout`, `SettingsLayout`)

Схема:

```
BaseWindow
 └── CentralWidget
      └── UIManager (QStackedLayout)
           ├── HomeLayout
           └── SettingsLayout
```

---

## 📂 Структура проекта

```
project/
│
├── app.py
├── core/
│   ├── base.py
│   └── ui_manager.py
│
├── layouts/
│   ├── home.py
│   └── settings.py
│
└── README.md
```

---

## 🚀 Запуск

### 1. Установить зависимости

```bash
pip install -r requirements.txt
```

### 2. Запустить приложение

```bash
python main.py
```

---

## 🛠️ Используемые технологии

* Python 3.11+
* PySide6 (Qt for Python)